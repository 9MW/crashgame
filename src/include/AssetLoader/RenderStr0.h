#pragma once

namespace pj2::n_Render {

    struct modelidn
    {
        std::string Name;
        int texstart, texsize, texsps, texspl, nds, ndl, lnds, lndl,
            mtls, mtlsize, anims, animl,sks,skl, exts, extl,Ids,Idl;
    };
}